#include<bits/stdc++.h>
using namespace std;

#define ll long long
const ll MOD = 1e9 + 7;

int main(){
    ll n;
    cin >> n;
    ll ans = 1;
    for( ll i = 1 ; i <= n ; i++ ){
        ans = ( ans * 2 ) % MOD;
    }
    cout << ans << endl;
}
